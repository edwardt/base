/*static void parse_options(int argc, char *argv[], options::variables_map& vm) 
{
  options::options_description desc("Options");
  desc.add_options()
    ("help,h", "print help message")
    ("daemon,d", "execute the program as a daemon")
  ;
  options::store(options::parse_command_line(argc, argv, desc), vm);
  options::notify(vm);
  }*/

